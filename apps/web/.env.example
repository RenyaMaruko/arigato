# API のベース URL（Hono RPC クライアントが叩く先）。未設定なら http://localhost:8787
VITE_API_URL=http://localhost:8787

# Google Analytics（GA4）の計測ID。未設定なら無効。
VITE_GA_ID=

# Stripe 公開可能キー（フロントの決済UI）。テストは pk_test_... 。公開前提だがコミットしない
VITE_STRIPE_PUBLISHABLE_KEY=
